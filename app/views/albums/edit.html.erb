<%= form_for @album,url: user_album_path(@user), method: :put, html: { class: 'form-group' } do |f| %>
  <div class="form-group">
    <%= f.label 'Album name' %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <%= f.submit 'pl' %>
<% end %>