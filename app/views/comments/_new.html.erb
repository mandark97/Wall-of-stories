<%= form_for Comment.new, url: story_comments_path(@story), remote: true, class: 'form-group' do |f| %>
  <%= f.text_field :text, class: 'form-control', placeholder: 'Your comment' %>
  <%= f.submit 'comment', class: 'btn btn-default' %>
<%end %>